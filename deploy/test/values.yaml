# Default values for kiva-ui service.

image:
  repository: kiva/ui
  tag: "" # Expected be set by Jenkins
  pullPolicy: Always

deployenv:
  environment: "" # set either by jenkins or manually

secretName: kiva-ui

replicaCount: 1

strategy:
  type: "RollingUpdate"

service:
  type: ClusterIP
  port: 8888
  portName: &servicePortName "http"

ingress:
  name: "kiva-ui-temp"
  host: "ui-test.dk1.kiva.org"
  annotations:
    cert-manager.io/cluster-issuer: ""
  spec:
    rules:
      - host: "a660dcce1f7d94e26bb5028c32267824-828904537.us-west-2.elb.amazonaws.com" # "ui-test.dk1.kiva.org"
        http:
          paths:
          - backend:
              serviceName: "kiva-ui"
              servicePort: *servicePortName
            path: /
  tls:
    - hosts:
      - ui-test.dk1.kiva.org #- ui-test.dk1.kiva.org
      secretName: "kiva-ui-no-secret" #secretName: "kiva-ui-tls"
