mutation RequestPayPalWithdrawal($amount: Money!, $paypalEmail: String!) {
    my {
        requestPayPalWithdrawal(amount: $amount, paypalEmail: $paypalEmail) {
            status
            message
        }
    }
}
