mutation validateGuestPreCheckout($basketId: String, $email: String, $visitorId: String) {
	shop (basketId: $basketId) {
		id
		validatePreCheckout (email: $email, visitorId: $visitorId) {
			error
			success
			value
		}
	}
}
