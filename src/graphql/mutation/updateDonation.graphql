mutation updateDonation($price: Money!, $isTip: Boolean!, $basketId: String, $metadata: String) {
	shop (basketId: $basketId) {
		id
		updateDonation (donation: {
			price: $price,
			isTip: $isTip,
			metadata: $metadata
		})
		{
			id
			price
			isTip
			metadata {
				campaignId
			}
		}
	}
}
