mutation createSavedSearch($name: String!, $filters: LoanSearchFiltersInput) {
  my {
    createSavedSearch (savedSearch :{
      filters: $filters,
      name: $name,
      isAlert: false,
      queryString: $name,
      sortBy: popularity
    })
    {
		id
    	name
	}
  }
}
