query userRepaymentTransactions($limit: Int, $offset: Int) {
    my {
        id
        transactions(limit: $limit, offset: $offset) {
            totalCount
            values {
                type
                amount
                effectiveTime

                loan {
                    id
                    name
                    image {
                        id
                        url
                        hash
                    }
                    geocode {
                        country {
                            id
                            name
                        }
                    }
                }
            }
        }
    }
}
