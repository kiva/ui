query categoryRows {
	lend {
		loanChannels (limit: 8, popular: true) {
			values {
				id
				url
				name
				image {
					id
					url (customSize: "w313h176")
				}
				retinaImage {
					id
					url (customSize: "w626h352")
				}
			}
		}
  	}
}
