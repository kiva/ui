query useGoalDataQuery($loanIds: [Int!]!) {
  my {
    id
	userPreferences {
      id
      preferences
    }
	loans {
	  totalCount
	}
  }
  postCheckoutAchievements(loanIds: $loanIds) {
	allTimeProgress {
		achievementId
        totalProgress
        currentTier
        completed
	}
  }
}
