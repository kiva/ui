query useGoalDataQuery {
  my {
    id
    userPreferences {
      id
      preferences
    }
    loans {
      totalCount
    }
  }
  general {
    goal_editing_enable: uiConfigSetting(key: "goal_editing_enable") {
			key
			value
		}
  }
}
