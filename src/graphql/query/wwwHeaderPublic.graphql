query wwwHeaderPublic {
	general {
		teamsMenuEnabled: featureSetting(key: "teams_in_navbar.enabled") {
			key
			value
		}
		multiMatchingEnabled: featureSetting(key: "create_multi_match_reservations.enabled") {
			key
			value
		}
	}
}
