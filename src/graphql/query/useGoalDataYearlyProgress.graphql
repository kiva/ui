query useGoalDataYearlyProgressQuery($year: Int) {
	userAchievementProgress(year: $year) {
        tieredLendingAchievements {
            id
            progressForYear
            totalProgressToAchievement
        }
    }
}
