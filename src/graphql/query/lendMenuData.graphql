query {
	my {
		userAccount {
			id
		}
	}
	loanChannels(popular: true, applyMinLoanCount: true, limit: 50) {
		values {
			index
			name
			url
		}
	}
	countryFacets {
		count
		country {
			name
			region
			isoCode
		}
	}
}
