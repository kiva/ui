query {
	general {
		experiment: setting(setting: "uiexp.register_fields") {
			key
			value
		}
		inviationParam: userSessionData(sessionKey: SocialInvitationParams) {
    		sessionKey
      		data
    	}
	}
}
