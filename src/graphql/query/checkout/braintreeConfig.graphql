query braintreeConfig {
	general {
		btVaultActive: uiConfigSetting(key: "feature.braintree_vault") {
			key
			value
		}
		btDataCollectorActive: uiConfigSetting(key: "feature.braintree_vault") {
			key
			value
		}
	}
}
