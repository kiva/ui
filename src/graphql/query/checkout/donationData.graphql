query {
	general {
		donationNudgeExperiment: setting(setting: "uiexp.donation_nudge_lightbox_custom_tip") {
			key
			value
		}
		donationNudgeLendingCostExperiment: setting(setting: "uiexp.donation_nudge_lending_cost") {
			key
			value
		}
		checkoutDonation100TextExperiment: setting(setting: "uiexp.checkout_donation_100_text") {
			key
			value
		}
		donationNudgeBorrowerImageExperiment: setting(setting: "uiexp.donation_nudge_borrower_image") {
			key
			value
		}
	}
	my {
		loans {
			totalCount
		}
	}
}
