query lendFilterPage($basketId: String) {
	my {
		userAccount {
			id
		}
	}
	shop (basketId: $basketId) {
		basket {
			items {
				values {
					id
				}
			}
		}
	}
	general {
		removeLastWords: uiExperimentSetting(key: "remove_words") {
			key
			value
		}
	}
}
