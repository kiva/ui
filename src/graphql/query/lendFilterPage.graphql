query lendFilterPage($basketId: String) {
	my {
		userAccount {
			id
		}
	}
	shop (basketId: $basketId) {
		id
		basket {
			id
			items {
				values {
					id
				}
			}
		}
	}
	general {
		lendFilterFlssExp: uiExperimentSetting(key: "lend_filter_flss_mock") {
			key
			value
		}
	}
}
