query {
	general {
		rows: setting(setting: "ui.category_rows") {
			value
		}
		rowsExp: setting(setting: "uiexp.category_rows") {
			key
			value
		}
		featuredExp: setting(setting: "uiexp.featured_loans") {
			key
			value
		}
		lendIncrementExp: setting(setting: "uiexp.lend_increment_button") {
			key
			value
		}
		recentlyViewedLoans: setting(setting: "uiexp.recently_viewed_loans") {
			key
			value
		}
		imageEnhancementExperiment: setting(setting: "uiexp.image_enhancement") {
			key
			value
		}
	}
	my {
		isAdmin
	}
	shop {
		basket {
			items {
				values {
					id
				}
			}
		}
	}
}
