#import '../fragments/loanCardFields.graphql'

query (
	$ids: [Int],
	$numberOfLoans: Int = 12,
	$imgDefaultSize: String = "w480h360",
    $imgRetinaSize: String = "w960h720",
) {
	lend {
		loanChannelsById(ids: $ids) {
			id
			name
			url
			loans(limit: $numberOfLoans, unique: true) {
				values {
					...loanCardFields
					image {
						id
						default: url(customSize: $imgDefaultSize)
						retina: url(customSize: $imgRetinaSize)
					}
				}
			}
		}
		# example aliased call to loans; place whatever query is necessary here
		# someLoans: loans(offset: 20, limit: 3) {
		# 	values {
		# 		...loanCardFields
		# 		image {
		# 			default: url(customSize: $imgDefaultSize)
		# 			retina: url(customSize: $imgRetinaSize)
		# 		}
		# 	}
		# }
		# example 2nd aliased call to loans; place whatever query is necessary here
		# otherLoans: loans(offset: 10, limit: 3) {
		# 	values {
		# 		...loanCardFields
		# 		image {
		# 			default: url(customSize: $imgDefaultSize)
		# 			retina: url(customSize: $imgRetinaSize)
		# 		}
		# 	}
		# }
	}
}
