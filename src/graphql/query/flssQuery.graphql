#import '../fragments/loanCardFields.graphql'

query basicLoanData ($sector: String = "retail") {
  fundraisingLoans(filters: { sector: { eq: $sector } }) {
    totalCount
    values {
      loan {
        ...loanCardFields
      }
    }
  }
}
