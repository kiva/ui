query LenderPublicProfile ($publicId: String!) {
	community {
		lender(publicId: $publicId) {
			id
			name
			loanCount
			image {
				id
				url
				width
				height
			}
			lenderPage {
				whereabouts
			}
		}
	}
}
