query (
	$country: [String] = ["US"],
	$activity: [Int] = [120],
	$imgDefaultSize: String = "w180h120",
	$imgRetinaSize: String = "w360h240"
) {
	lend {
		sameCountry: loans(filters: {country: $country}, limit: 1, sortBy: random) {
			values {
				id
				name
				loanAmount
				loanFundraisingInfo {
					fundedAmount
				}
				image {
					id
					default: url(customSize: $imgDefaultSize)
					retina: url(customSize: $imgRetinaSize)
				}
				activity {
					id
					name
				}
				geocode {
					country {
						isoCode
						name
					}
				}
			}
		}

		sameActivity: loans (filters: {activity: $activity}, limit: 1, sortBy: random) {
			values {
				id
				name
				loanAmount
				loanFundraisingInfo {
					fundedAmount
				}
				image {
					id
					default: url(customSize: $imgDefaultSize)
					retina: url(customSize: $imgRetinaSize)
				}
				activity {
					id
					name
				}
				geocode {
					country {
						isoCode
						name
					}
				}
			}
		}

		randomLoan: loans (limit: 3, sortBy: random) {
			values {
				id
				name
				loanAmount
				loanFundraisingInfo {
					fundedAmount
				}
				image {
					id
					default: url(customSize: $imgDefaultSize)
					retina: url(customSize: $imgRetinaSize)
				}
				activity {
					id
					name
				}
				geocode {
					country {
						isoCode
						name
					}
				}
			}
		}
	}
}
