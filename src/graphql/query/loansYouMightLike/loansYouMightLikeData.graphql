query (
	$country: [String],
	$activity: [Int],
	$sector: [Int],
	$imgDefaultSize: String = "w480h360",
	$imgRetinaSize: String = "w480h360",
) {
	lend {
		sameCountry: loans(filters: {country: $country, status: fundRaising}, limit: 2, sortBy: random) {
			values {
				id
				name
				loanAmount
				loanFundraisingInfo {
					fundedAmount
					reservedAmount
				}
				image {
					id
					default: url(customSize: $imgDefaultSize)
					retina: url(customSize: $imgRetinaSize)
				}
				activity {
					id
					name
				}
				geocode {
					country {
						isoCode
						name
					}
				}
				sector {
          			id
          			name
        		}
			}
		}

		sameActivity: loans (filters: {activity: $activity, status: fundRaising}, limit: 2, sortBy: random) {
			values {
				id
				name
				loanAmount
				loanFundraisingInfo {
					fundedAmount
					reservedAmount
				}
				image {
					id
					default: url(customSize: $imgDefaultSize)
					retina: url(customSize: $imgRetinaSize)
				}
				activity {
					id
					name
				}
				geocode {
					country {
						isoCode
						name
					}
				}
				sector {
          			id
          			name
        		}
			}
		}

		sameSector: loans(filters: {sector: $sector, status: fundRaising}, limit: 2, sortBy: random) {
			values {
				id
				name
				loanAmount
				loanFundraisingInfo {
					fundedAmount
					reservedAmount
				}
				image {
					id
					default: url(customSize: $imgDefaultSize)
					retina: url(customSize: $imgRetinaSize)
				}
				activity {
					id
					name
				}
				geocode {
					country {
						isoCode
						name
					}
				}
				sector {
          			id
          			name
        		}
			}
		}

		randomLoan: loans (filters: {status: fundRaising}, limit: 4, sortBy: random) {
			values {
				id
				name
				loanAmount
				loanFundraisingInfo {
					fundedAmount
					reservedAmount
				}
				image {
					id
					default: url(customSize: $imgDefaultSize)
					retina: url(customSize: $imgRetinaSize)
				}
				activity {
					id
					name
				}
				geocode {
					country {
						isoCode
						name
					}
				}
				sector {
          			id
          			name
        		}
			}
		}
	}
}
