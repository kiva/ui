query {
	my {
		userAccount {
			id
		}
	}
	lend {
		loanChannels(offset:0, limit:1000, popular: false, applyMinLoanCount: false) {
			totalCount
			values {
				id
        		url
			}
		}
	}
	shop {
		basket {
			items {
				values {
					id
				}
			}
		}
	}
}
