query {
	my {
		userAccount {
			id
		}
	}
	lend {
		loanChannels(offset:0, limit:1000, popular: false, applyMinLoanCount: false) {
			totalCount
			values {
				id
				url
			}
		}
	}
	general {
		addToBasketPopup: setting(setting: "uiexp.add_to_basket_popup") {
			key
			value
		}
	}
}
