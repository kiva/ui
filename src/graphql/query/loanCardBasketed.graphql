query loanCardBasketed($id: Int!, $basketId: String) {
	shop (basketId: $basketId) {
		id
		nonTrivialItemCount
		basket {
			id
			items {
				values {
					id
					... on Donation {
						id
						isTip
					}
				}
			}
		}
	}
	lend {
		loan(id: $id) {
			id
			minNoteSize
			loanFundraisingInfo {
				id
				fundedAmount
				reservedAmount
				isExpiringSoon
			}
		}
	}
}
