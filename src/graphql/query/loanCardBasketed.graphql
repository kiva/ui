#import '../fragments/loanCardFields.graphql'

query($id: Int!) {
	shop {
		nonTrivialItemCount
		basket {
			items {
				values {
					id
				}
			}
		}
	}
	lend {
		loan(id: $id) {
			...loanCardFields
		}
	}
}
