query($team_id: Int, $team_recruitment_id: Int) {
	community {
		team(id: $team_id) {
			name
			membershipType
		}
	}
	my {
		teamRecruitment(id: $team_recruitment_id) {
			recruiterDisplayName
		}
	}
}
