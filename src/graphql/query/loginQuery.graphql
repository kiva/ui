query loginQuery {
	general {
		salesforceSolution(id: "50150000000TCyi") {
			name
			note
		}
		inviteCode: userSessionData(sessionKey: InviteSecurityCode) {
			sessionKey
			data
		}
		inviteParams: userSessionData(sessionKey: SocialInvitationParams) {
			sessionKey
			data
		}
	}
}
