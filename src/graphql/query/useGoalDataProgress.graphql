query useGoalDataProgressQuery($loanIds: [Int!]!, $year: Int!) {
  postCheckoutAchievements(loanIds: $loanIds, year: $year) {
	yearlyProgress {
		achievementId
        totalProgress
        currentTier
        completed
	}
  }
}
